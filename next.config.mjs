/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['eatsmart-fit5120.s3.ap-southeast-2.amazonaws.com', 'images.unsplash.com', 'img.b2bpic.net'],
	},
};

export default nextConfig;
